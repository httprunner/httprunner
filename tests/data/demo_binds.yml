bind_variables:
    variables:
        - TOKEN: "debugtalk"
        - token: $TOKEN

builtin_functions:
    variables:
        - length: ${len(debugtalk)}
        - smallest: ${min(2, 3, 8)}
        - largest: ${max(2, 3, 8)}
